const Pokemon = require('../database/models/Pokemon');

function redeemPokemon(user, pokemonId) {
  // Redemption logic
}

function spawnPokemon(pokemonId, IVStats) {
  // Spawn Pokemon logic
}

module.exports = { redeemPokemon, spawnPokemon };